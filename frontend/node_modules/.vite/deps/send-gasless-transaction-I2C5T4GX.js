import "./chunk-MVEJMUOB.js";

// node_modules/thirdweb/dist/esm/transaction/actions/gasless/send-gasless-transaction.js
async function sendGaslessTransaction({ account, transaction, serializableTransaction, gasless }) {
  if (serializableTransaction.value && serializableTransaction.value > 0n) {
    throw new Error("Gasless transactions cannot have a value");
  }
  if (gasless.provider === "biconomy") {
    const { relayBiconomyTransaction } = await import("./biconomy-2TDZGCQH.js");
    return relayBiconomyTransaction({
      account,
      transaction,
      serializableTransaction,
      gasless
    });
  }
  if (gasless.provider === "openzeppelin") {
    const { relayOpenZeppelinTransaction } = await import("./openzeppelin-RLGCOIRF.js");
    return relayOpenZeppelinTransaction({
      account,
      transaction,
      serializableTransaction,
      gasless
    });
  }
  if (gasless.provider === "engine") {
    const { relayEngineTransaction } = await import("./engine-DUYTKQ5A.js");
    return relayEngineTransaction({
      account,
      transaction,
      serializableTransaction,
      gasless
    });
  }
  throw new Error("Unsupported gasless provider");
}
export {
  sendGaslessTransaction
};
//# sourceMappingURL=send-gasless-transaction-I2C5T4GX.js.map
